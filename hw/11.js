11. // Types, Values, and Variables
    // Instruction:
    // - No coding, What is the result of the code below, and why is it displayed? Initiate code:
// XXXXX : What is you think about result

const arr = [1, 2, 3];
arr.push(4); // หนูคิดว่าเพิ่ม 4 มาต่อท้าย
arr = [1, 2, 3, 4]; // ใช่

const obj = { name: 'Alice' };
obj.age = 30; // เพิ่ม key = age ที่มี value = 30 เพิ่มเข้ามาใน obj
obj = { name: 'Alice', age: 30 }; // ใช่