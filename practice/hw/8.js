8. // Expressions, Operators, and Control Structures
    // Instruction:
    // - No coding, What is the result of the code below, and why is it displayed? 
    // Initiate code:
        // XXXXX : What is you think about result

if(1){
    console.log(1); // 1 เพราะเป็นจริง
}

if([]){
    console.log([]); // [] เพราะเป็นจริง
}

if(""){
    console.log(""); // เป็นเท็จ ไม่แสดง
} 

if(null){
    console.log(null); // เป็นเท็จ ไม่แสดง
}

if(undefined){ 
    console.log(undefined); // เป็นเท็จ ไม่แสดง
}

// 0 , null , undefined , NaN มีค่าเป็น false